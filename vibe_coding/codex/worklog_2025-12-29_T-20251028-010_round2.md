# T-20251028-010 Deck Ideal MCP 恢复尝试记录（Round 2）

## 目标
- 恢复 Unity MCP 连接，完成 Console 编译错误双次拉取与 Deck Ideal 自动化截图产出。

## 尝试过程与思路
- MCP 直连测试：
  - `read_console` 返回错误：`No Unity plugins are currently connected`。
  - `unity://instances` 显示 instance_count=0，确认 Unity 插件未接入。
- 重启 Unity 以恢复连接：
  - 多次 Stop/Start Unity 进程，期望插件重新握手。
  - Unity 进程启动后仍无实例连接，MCP 资源读取继续失败。
- 试图用 batchmode 触发截图：
  - 使用 `-executeMethod MageKnight.EditorTools.DeckIdealSceneBuilder.BuildAndCaptureMenu` 启动 Unity。
  - 由于 `-logFile` 未生成/未更新，无法确认执行进度；截图目录仍为空。
- 哨兵文件检查：
  - `multi-agent-workspace/compile/deckideal_autocapture.flag` 仍存在，说明自动截图未触发或未完成。

## 当前结果
- MCP 仍未恢复连接，无法按标准流程拉取 Console errors。
- Deck Ideal 截图未生成：`multi-agent-workspace/runs/T-20251028-010/review_bundle/artifacts/screenshots` 为空。
- 已写入 `compile_status.json` 为 `{"ok": false}`，避免误报通过。

## 下一步策略
- 需要人工确认 Unity 是否因许可/弹窗/编译阻塞导致插件无法连接。
- 建议手动启动 Unity 并确保项目完整加载后，再次尝试 MCP `read_console` 与 `execute_menu_item`。
- MCP 恢复后按标准流程执行：
  - 双次 `read_console` errors=0；
  - 运行 Deck Ideal Automation 或 Build + Capture；
  - 生成截图并更新 compile_status.json 为 ok=true。
