# T-20251028-012 Round5 Worklog

## 目标
- 解决 Unity Console 的 MCP WebSocket error，确保编译检查 error=0。
- 清理 DeckIdeal 点击高亮的大块半透明遮罩，保证截图不遮挡卡面/场地纹理。
- 在 Unity 编辑器内重新生成 DeckIdeal 自动化产物（同一 run 目录）。

## 修改思路
- 通过 EditorPrefs 强制 MCP 使用 stdio transport，避免 WebSocket 连接失败产生 error。
- 将高亮从“整块填充”改为“描边式高亮”，并降低透明度，避免截图出现遮罩感。

## 已完成
- 新增 `Assets/Editor/McpTransportPrefs.cs`，在编辑器加载时关闭 HTTP/WebSocket transport。
- 调整 `DeckIdealSceneBuilder` 的高亮透明度并禁用 fillCenter。
- 调整 `DeckIdealHitboxHighlighter` 的激活 alpha，减轻遮罩感。
- 新增 `tools/mcp_stdio_request.ps1`，通过 stdio bridge 调用 execute_menu_item / read_console。
- MCP 传输策略改为强制 stdio，避免 WebSocket 连接失败错误。

## 待执行
- （已完成）Assets/Refresh -> Console error=0 (x2)。
- （已完成）Run DeckIdeal Automation，报告 success，截图已更新到 T-20251028-012。
- 更新 compile_check 与经验索引。

## 记录
- Compile check: vibe_coding/codex/compile_check_20260106_032022.md
- Report: multi-agent-workspace/runs/T-20251028-012/review_bundle/artifacts/deck_ideal_report.json (status=success)
- 重建场景：Tools/Scene Builders/Build + Capture Deck Ideal 后再跑自动化，截图时间更新至 03:23。
