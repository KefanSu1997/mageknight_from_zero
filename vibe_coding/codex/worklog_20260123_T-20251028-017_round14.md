# Worklog 2026-01-23 - T-20251028-017 (Round 14)

Goal:
- Restore MCP console access and capture compile error snapshot.

Changes:
- Switched MCP transport prefs to stdio (disable HTTP) so Unity opens the socket listener.
- Adjusted MCP auto-start to force stdio transport and resume after reload.

Next:
- Trigger Unity refresh, then retry read_console until it succeeds.
- Capture error count (types=error, count>=1000, include_stacktrace=true) twice.
Update:
- MCP tool calls still return tool error (execute_menu_item/read_console).
- Unity does not open port 6400 and no unity-mcp-port-*.json is created yet.
- Registry updated to disable HTTP transport and enable stdio resume, waiting for Unity reload.
