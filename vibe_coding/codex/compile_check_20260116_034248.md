# Compile Check Log (20260116_034248)

- Target: MageKnight_from_zero
- MCP server: http://localhost:60022/mcp
- Result: unable to fetch console errors

Attempts:
- mcp__unityMCP__read_console (action=get, types=[error], include_stacktrace=true) -> HTTP 400 Bad Request
- mcp__unityMCP__debug_request_context -> HTTP 400 Bad Request

Notes:
- Unity MCP connectivity appears broken; port 60022 reachable but requests return 400.
- No compile error count can be recorded until MCP console access is restored.
