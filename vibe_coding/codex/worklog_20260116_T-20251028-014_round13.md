Round 13 worklog (2026-01-16)

Goal
- Restore Unity MCP connectivity to run compile checks and automation for ElementCards.

What I changed
- Added retry loop to MCP bridge auto-start to re-attempt HTTP transport startup until the server is available.
  File: Assets/Editor/McpBridgeAutoStart.cs

Investigation notes
- MCP server now reachable via HTTP (rmcp calls succeed).
- WebSocket hub is reachable at ws://127.0.0.1:60022/hub/plugin (manual client test).
- Unity Editor log shows earlier MCP WebSocket registration and verification entries, but current /plugin/sessions returns empty.
- Assembly-CSharp-Editor.dll timestamp is old; recent C# edits did not recompile (auto-refresh likely not triggering).
- CcrCompileBridge did not process new request_*.json files (no ack/result), suggesting editor update loop not running or scripts not reloaded.

Open issues
- Unity MCP plugin is not connected to the current server process (plugin sessions empty).
- Need a reliable way to trigger a domain reload or start HTTP bridge inside Unity without CLI/batchmode.

Next steps
- Trigger Unity script reload manually (e.g., editor refresh) so McpBridgeAutoStart retry loop runs.
- Verify /plugin/sessions returns a Unity session, then proceed with compile check and ElementCards automation.
