# 持续修复原卡规则与场景验收

## 目标及基线

用户要求持续迭代，直到所有卡牌测试通过。范围包括121张有效行动/法术/宝物牌面，以及需进一步核实的32个部队、82个技能条目；3张空图和1张牌背保留数据问题标记，不冒充可用卡。不得删除失败案例、放宽原规则预期或将partial改名passed。

从origin/main建立feat/card-rules-completion，再在非当前分支上快进接入已存档2a43cdd，最后切换同一文件树，避免在运行的Unity工程中来回重写Assets。保留运行时字体缓存本地修改。

基线312场景案例：75通过、111不符、126待联验。91项EditMode测试及四个冒险场景回归通过。

## 第一批：伤牌与治疗守恒

- 将PlayerState.Wounds/DiscardWounds改为读取真实手牌/弃牌区，兼容setter也直接增减真实伤牌，消除独立计数漂移。
- 移除AbilityRules中计数增加后再次添加伤牌的重复路径；毒素只在弃牌区额外加入一次伤牌。
- 引入CardHealing.Heal，统一按实际可移除伤牌数量结算治疗及DrawPerHeal。现有治疗效果调用同一服务；痊愈、黄金圣杯先激活本回合触发，再结算自身治疗。
- 审计初始夹具只创建一次伤牌；界面初始状态改为读取实际手牌与各色魔力，避免边界夹具仍显示固定6手牌的错误说明。
- 新增4项独立伤牌守恒测试；场景追加宁静无伤牌、治疗超出伤牌数量与抽牌触发边界，基础116例、全批316例，保留原312案例不变。
- 修改后待Unity编译、Console双扫描、全批场景与回归结果。每一批完成后补记结果，目标未完成前不标记完成。

## 验收报告写入中断

2026-09-10 13:53（本地）首轮修复重跑在基础第4例中断：Console IOException Win32 IO 1224，effects.json 被映射占用时 File.WriteAllText 无法截断。不能据此认定规则失败或将旧报告拼接为本轮通过。

修复：每次菜单启动分配时间戳+GUID独立输出目录，通过SessionState跨场景保留；每例写不可变checkpoint，全部完成后同目录临时文件原子发布effects.json。UI报告和效果报告使用同一运行目录，汇总脚本通过--output显式指定。保留失败目录和原始异常证据。

## 第一批实际验收结果

运行目录：`AutomationOutputs/AllOriginalCards/20260910_140343_c3df5a04`。

- 316例完整场景执行：85 passed、92 failed、139 partial。相较312例基线，新增4例全部通过，旧失败净减少19例；partial仍不是整卡通过。
- 基础116例：70/27/19；高级102例：15/18/69；法术48例：0/40/8；宝物50例：0/7/43（依次passed/failed/partial）。
- 实际944次按钮点击、2792个效果断言、320张原卡截图；汇总脚本核对案例集合、报告时间、点击命中与每例截图。
- 95项EditMode测试全部通过，0失败、0跳过；新增4项伤牌守恒测试包含其中。
- 四个冒险场景回归：114次操作、477个断言、118张1920×1080截图全部通过；独立运行报告和截图复制到本轮regression目录。
- Unity脚本实际编译后Console两次0error；本轮场景与回归结束后再次拉取Console两次0error，证据为console_final_1/2.json。
- 目视检查宁静治疗/无伤牌边界、黄金圣杯治疗及名望、痊愈治疗/抽牌数值，以及冒险结局。痊愈仍明确显示绿色施法费用未扣，不把治疗修复当作整卡通过。
- 未提供有效fmt/lint脚本（package.json只有占位test），使用C#空白检查、git diff --check、Python AST、JSON解析，以及实际Unity编译/测试作为本批检查。

下一批：按原卡图修复法术颜色与上下半部费用；支付失败保持所有颜色不变、强化费用不足明确拒绝；绿色晶化使用ManaColor支付，不再通过攻击Element转换。随后继续完整牌区生命周期、目标合法性、实际移动/战斗消费者以及部队/技能场景。
