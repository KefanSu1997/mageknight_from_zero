# Worklog: T-20251028-012 Round 10 (2026-01-07)

## Goal
- Remove all large semi-transparent rectangles in DeckIdeal screenshots (top-left, top-right, center, bottom).

## Changes
- Reduced/removed overlay visibility in `Assets/Editor/DeckIdealSceneBuilder.cs`:
  - MagicCircleColor/SwirlColor alpha -> 0.
  - Deck/Discard/Hand highlight alpha -> 0.
  - Shelf/BottomBand Base already set to fillCenter=false and alpha 0.
- Directly patched `Assets/Scenes/Part1/Part1_DeckIdeal.unity` to force alpha=0 for:
  - MagicCircleOverlay, MagicSwirl, DeckHighlight, DiscardHighlight, HandHighlight.
- Added experience note about JPG overlay rectangles and updated experience index.

## Verification
- Unity MCP tools became unavailable (execute_menu_item/read_console failed), so:
  - Could not rebuild DeckIdeal via menu.
  - Could not run Scene Automation.
  - Could not run compile error checks via Console.

## Next Steps
- Reconnect Unity MCP (restart Unity if needed).
- Run `Tools/Scene Builders/Build + Capture Deck Ideal`.
- Run `Tools/Scene Automation/Run DeckIdeal Automation`.
- Perform standard Console error checks (twice) and log results.
- Re-check screenshots: deck_ideal_overview.png / 000_Scene Start.png / 003_Hand.png.
