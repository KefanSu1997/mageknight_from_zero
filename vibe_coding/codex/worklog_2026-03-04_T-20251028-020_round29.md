# Round 29 Worklog (T-20251028-020)

## Goal
- Fix final blockers for ElementCardsShowcase evidence:
- Remove cross-card leakage caused by overlapping crop rectangles.
- Remove visible white edges in back-side captures.
- Produce one fresh single-run evidence window with readable C# diff.

## Changes Implemented
- Updated `Assets/Scripts/SceneAutomation/ElementCardsManualCaptureRunner.cs` crop pipeline:
- Increased crop safety inset (`StepCropInsetMinPixels`, `StepCropInsetRatio`) to avoid edge bleed.
- Replaced per-step crop computation with target-card screen rect + neighbor centerline lane boundaries.
- Added horizontal lane clamp (`StepCropLaneGapPixels`) so each element crop is isolated from adjacent cards even if UI cards overlap.
- Added richer crop log payload (`laneDividers`, `laneRange`, `imageRect`) for auditability.
- Kept screenshot uniqueness gate and back/front pair hash gate unchanged.

## Readable C# Diff Excerpt
```diff
- private const float StepCropInsetMinPixels = 4f;
- private const float StepCropInsetRatio = 0.015f;
+ private const float StepCropInsetMinPixels = 14f;
+ private const float StepCropInsetRatio = 0.03f;
+ private const float StepCropLaneGapPixels = 2f;
```

```diff
- if (!TryBuildStepCropRect(stepLabel, elementIndex, width, height, out var cropRect, out var rectMessage))
+ if (!TryBuildStepCropRect(stepLabel, width, height, out var cropRect, out var rectMessage))
```

```diff
+ var targetCenterX = screenRect.center.x;
+ var leftDivider = float.NegativeInfinity;
+ var rightDivider = float.PositiveInfinity;
+ foreach (var pair in screenRectsByElement)
+ {
+     if (string.Equals(pair.Key, elementLabel, StringComparison.OrdinalIgnoreCase)) continue;
+     var otherCenterX = pair.Value.center.x;
+     if (otherCenterX < targetCenterX) leftDivider = Mathf.Max(leftDivider, (otherCenterX + targetCenterX) * 0.5f);
+     else if (otherCenterX > targetCenterX) rightDivider = Mathf.Min(rightDivider, (otherCenterX + targetCenterX) * 0.5f);
+ }
+ if (!float.IsNegativeInfinity(leftDivider)) xMinScreen = Mathf.Max(xMinScreen, leftDivider + StepCropLaneGapPixels);
+ if (!float.IsPositiveInfinity(rightDivider)) xMaxScreen = Mathf.Min(xMaxScreen, rightDivider - StepCropLaneGapPixels);
```

## Automation Run
- Triggered one run via Unity menu:
- `Tools/Scene Automation/Run ElementCardsShowcase Automation`
- Output status:
- `review_bundle/artifacts/element_cards_report.json` => `status=success`, `steps=9`
- Fresh screenshots generated:
- `000_Scene Start.png`
- `001_Earth Back.png` ... `008_Fire Front.png`
- Pairwise hash checks:
- `001!=002`, `003!=004`, `005!=006`, `007!=008` => all `True`

## Visual Verification
- New back captures (`001/003/005/007`) no longer show neighboring cards.
- White side/bottom strips visible in previous run are removed in new crops.
- Front captures (`002/004/006/008`) still match the expected Earth/Water/Wind/Fire faces.

## Compile Guardrail
- Followed console-error dual-scan process (`types=[\"error\"], count=1000, include_stacktrace=true`).
- Scan #1: 0 errors.
- Scan #2: 0 errors.
- Wrote identical structured compile evidence to both required paths:
- `multi-agent-workspace/runs/T-20251028-020/compile_status.json`
- `multi-agent-workspace/runs/T-20251028-020/review_bundle/artifacts/compile_status.json`

## Notes
- During playmode transition the active MCP instance id rotated once; recovery used:
- `mcpforunity://instances` -> `set_active_instance(...)`
- Then continued polling and verification without sleep commands.
